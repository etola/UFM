dataset_str:
  "FlyingThings3D(
    split='${dataset.flyingthings.val.split}',
    resolution=${dataset.flyingthings.val.dataset_resolution},
    seed=${dataset.flyingthings.val.seed},
    transform='${dataset.flyingthings.val.transform}',
    data_norm_type='${dataset.flyingthings.val.data_norm_type}',
    ROOT='${dataset.flyingthings.val.ROOT}',
    dstype='${dataset.flyingthings.train.dstype}')"
split: 'test'
dstype: 'clean'
dataset_resolution: ${dataset.resolution_val}
seed: 777
transform: 'imgnorm'
data_norm_type: ${model.data_norm_type}
ROOT: ${root_data_dir}/flyingthings3d_processed
