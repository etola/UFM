dataset_str:
  "Habitat(
    split='${dataset.habitat.val.split}',
    size='${dataset.habitat.val.size}',
    resolution=${dataset.habitat.val.dataset_resolution},
    seed=${dataset.habitat.val.seed},
    transform='${dataset.habitat.val.transform}',
    data_norm_type='${dataset.habitat.val.data_norm_type}',
    ROOT='${dataset.habitat.val.ROOT}')"
split: 'val'
size: 3500
dataset_resolution: ${dataset.resolution_val}
seed: 777
transform: 'imgnorm'
data_norm_type: ${model.data_norm_type}
ROOT: ${root_data_dir}/habitat_processed/replica_cad_baked_lighting
