dataset_str:
  "MegaDepth(
    split='${dataset.megadepth.train.split}',
    resolution=${dataset.megadepth.train.dataset_resolution},
    aug_crop='${dataset.megadepth.train.aug_crop}',
    aug_monocular=${dataset.megadepth.train.aug_monocular},
    transform='${dataset.megadepth.train.transform}',
    data_norm_type='${dataset.megadepth.train.data_norm_type}',
    ROOT='${dataset.megadepth.train.ROOT}',
    pair_metadata_path='${dataset.megadepth.train.pair_metadata_path}',
    aug_low_light=False)"
split: 'train'
dataset_resolution: ${dataset.resolution_train}
aug_crop: auto_crop_asis
aug_monocular: 0.005
transform: 'colorjitter'
data_norm_type: ${model.data_norm_type}
ROOT: ${root_data_dir}/megadepth_processed
pair_metadata_path: ${root_data_dir}/dust3r_data_pairs/megadepth_pairs.npz
