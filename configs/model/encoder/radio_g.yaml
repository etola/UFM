defaults:
  - default

encoder_str: radio
patch_size: 14
encoder_embed: 1536

encoder_kwargs:
  name: radio_encoder
  data_norm_type: radio
  patch_size: ${model.encoder.patch_size}
  model_version: radio_v2.5-g
